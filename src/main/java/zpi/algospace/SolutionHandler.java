package zpi.algospace;

import java.util.UUID;

class SolutionHandler {
    public static void handle(Solution solution) {
        String className = Language.JAVA.getLanguageName() + String.valueOf(UUID.randomUUID()).replace("-", "");


    }
}
